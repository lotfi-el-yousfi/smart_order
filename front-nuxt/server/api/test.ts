 export default eventHandler(async (event) => {
   // const body = await readBody(event); // Read the request body
   // const data: IAnswerPost = body.data; // Extract data from the body

   // return await createAnswer(data, user.id);
});